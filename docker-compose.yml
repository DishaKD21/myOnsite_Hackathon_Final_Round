services:
  backupchain:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - ./data:/app/data
  frontend:
    build:
      context: ../frontend
      args:
        VITE_API_BASE_URL: http://localhost:8080
    ports:
      - "5174:80"
    depends_on:
      - backupchain
